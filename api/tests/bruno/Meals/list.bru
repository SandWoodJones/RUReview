meta {
  name: List meals
  type: http
  seq: 1
}

get {
  url: {{base_url}}/meals
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("status 200", function () {
    expect(res.status).to.equal(200);
  });
  test("data e uma lista", function () {
    expect(res.body.data).to.be.an("array");
  });
}

docs {
  Rota publica. Adicione ?date=2026-06-25 na URL para filtrar por data.
}
